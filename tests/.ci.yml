prepare:
- echo `date` > /tmp/op1
- echo `hostname` > /tmp/op2
- mktemp
- exit 2

jobs:
  test1:
    - touch /tmp/fail_test1
    - exit 1
  build_doc:
    - touch /tmp/build_doc

deploy:
  - cat /tmp/op* > /tmp/deployed
