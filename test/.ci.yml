prepare:
- echo `date` > /tmp/op1
- echo `hostname` > /tmp/op2
- mktemp
- exit 2

test:
  test1:
    - ls /tmp/op*
    - exit 1
  build_doc:
    - ls /tmp/tmp.*

deploy:
  - cat /tmp/op* > /tmp/deployed